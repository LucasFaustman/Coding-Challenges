// Write a function that will compare two values, one will be a number and one will be a string. Return true if they are the same character (regardless of their different data types) and return false if they are not.

function add(a, b){
    //simple 2 equals sign comparison
    return a == b
  }
  //P: will be 2 params, one a string and one a number. will always be a string and number
  // R; return a boolean representing if the 2 params are equal in value, not type
  // E: '1',1 => true